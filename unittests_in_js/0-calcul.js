// Task 0 - create a function calculateNumber that accepts two args (a, b)
// and returns the sum of a and b

function calculateNumber(a, b) {
  return Math.round(a) + Math.round(b);
}

module.exports = calculateNumber;

